# ☀️ Умный Двухосевой Солнечный Трекер

<div align="center">

![Platform](https://img.shields.io/badge/Platform-ESP32-blue?style=for-the-badge&logo=espressif)
![Framework](https://img.shields.io/badge/Framework-Arduino%20%2B%20FreeRTOS-red?style=for-the-badge)
![Language](https://img.shields.io/badge/Language-C%2B%2B-purple?style=for-the-badge&logo=cplusplus)
![Build](https://img.shields.io/badge/Build-PlatformIO-orange?style=for-the-badge)

**Автономная система двухосевого слежения за Солнцем на базе ESP32 и FreeRTOS**

*Астрономические алгоритмы NOAA · Веб-интерфейс SPA · Ночной режим энергосбережения*

</div>

---

## 📖 Описание проекта

Данный проект — это **автономная система слежения за Солнцем (Solar Tracker)** на двух осях. Устройство автоматически вычисляет положение Солнца на небосклоне с помощью астрономических алгоритмов (библиотека NOAA) и направляет солнечные панели точно на него для получения **максимального КПД**.

Корпус и механические узлы спроектированы в 3D и напечатаны на профессиональном 3D-принтере **Bambu Lab P2S**. Мозгом системы выступает микроконтроллер **ESP32**, работающий под управлением операционной системы реального времени **FreeRTOS**.

---

## ✨ Ключевые особенности

| Особенность | Описание |
|---|---|
| 🧠 **Двухъядерная архитектура** | **Ядро 0** — целиком отвечает за веб-сервер и Wi-Fi. **Ядро 1** — управляет механикой и астрономическими расчётами. Никаких зависаний. |
| 📐 **Алгоритм NOAA** | Система **не использует фоторезисторы**. Она получает точное время по NTP и математически вычисляет азимут и высоту Солнца по GPS-координатам. |
| 🌙 **Ночной режим** | Когда Солнце заходит (`altitude ≤ 0°`), система **отключает питание сервоприводов** (`.detach()`), исключая расход энергии. Утром — плавный выход из сна. |
| ⚡ **Встроенный вольтметр** | Измерение реального напряжения панелей через АЦП (12-бит) с делителем напряжения (R1=10кОм, R2=4.7кОм). |
| 🌐 **Web SPA-интерфейс** | Современный одностраничный дашборд, доступный с телефона или ПК. Обновление данных каждые **2 секунды** через REST API. |
| 💾 **EEPROM-конфигурация** | Все настройки (Wi-Fi, GPS-координаты, GMT, ограничения углов) сохраняются в энергонезависимую память ESP32. |
| 🔄 **Smooth Move** | Плавное перемещение сервоприводов при пробуждении с задержкой 40 мс/шаг — без рывков и механических ударов. |
| 📡 **Serial Monitor** | При включении устройство выводит **IP адрес** в порт (115200 бод) — удобно для первичного обнаружения. |

---

## 🔧 Аппаратная часть (Hardware)

### Компоненты

| Компонент | Характеристики |
|---|---|
| **Микроконтроллер** | ESP32 DevKit V4 (2 ядра, 240 МГц, Wi-Fi 802.11 b/g/n) |
| **Сервопривод горизонта** | MG996R — **Пин GPIO5** |
| **Сервопривод вертикали** | MG996R — **Пин GPIO18** |
| **Вход вольтметра** | АЦП — **Пин GPIO34** |
| **Солнечные панели** | 4 шт. (115×115 мм, 6В, 330 мА каждая) · Соединение: **параллельное** |
| **Пиковая мощность** | ~6В · ~1.32А · **≈7.9 Вт** |
| **Контроллер заряда** | TP4056 / H961-U |
| **Аккумулятор** | Li-Ion **10 000 мАч** |

### Схема делителя напряжения (Вольтметр)

```
[Solar Panel +] ──┬── R1 (10 кОм) ──── [GPIO34 (ADC)] ──── R2 (4.7 кОм) ──── [GND]
                  └── к нагрузке
```

> **Макс. измеряемое напряжение:** `3.3 В × (10000 + 4700) / 4700 ≈ 10.3 В`

---

## 🗂️ Структура проекта

```
ESP32_MG996R_Solar_Algorithm/
├── src/
│   └── main.cpp          # Весь исходный код прошивки
├── include/              # Заголовочные файлы
├── lib/                  # Локальные библиотеки
├── test/                 # Тесты
├── platformio.ini        # Конфигурация сборки PlatformIO
└── README.md
```

---

## 🏗️ Архитектура программного обеспечения

Прошивка построена на **двухзадачной FreeRTOS архитектуре** с защитой общих данных через `SemaphoreHandle_t` (Mutex).

```
┌─────────────────────────────────────────────────────┐
│                    ESP32 (FreeRTOS)                 │
│                                                     │
│  ┌──────────────────┐    ┌───────────────────────┐  │
│  │  ЯДРО 0 (Core 0) │    │  ЯДРО 1 (Core 1)      │  │
│  │  TaskWeb         │    │  TaskTracker           │  │
│  │                  │    │                        │  │
│  │  • WebServer     │    │  • Расчёт SunPosition  │  │
│  │  • REST API      │◄──►│  • Управление Servo    │  │
│  │  • WiFi Handler  │ ↕  │  • АЦП вольтметр      │  │
│  │  • OTA Reboot    │ М  │  • Ночной режим        │  │
│  └──────────────────┘ U  └───────────────────────┘  │
│                       T                             │
│              Общие данные (Mutex):                  │
│       mode, sunAz, sunAlt, panelVolts,              │
│       currentHor, currentVer, isNight               │
└─────────────────────────────────────────────────────┘
```

### REST API эндпоинты

| Метод | Маршрут | Описание |
|---|---|---|
| `GET` | `/` | Главная страница (SPA-интерфейс) |
| `GET` | `/api/status` | Статус системы в JSON (время, вольты, углы, режим) |
| `GET` | `/api/setMode?mode={0-3}` | Смена режима работы |
| `GET` | `/api/setManual?h={deg}&v={deg}` | Ручное управление сервоприводами |
| `GET` | `/api/scan` | Сканирование доступных Wi-Fi сетей |
| `GET` | `/api/saveCfg?...` | Сохранение конфигурации в EEPROM |

---

## ⚙️ Режимы работы

| №  | Режим | Описание |
|----|-------|----------|
| `0` | **АВТО (СЛЕЖЕНИЕ)** | Основной режим. Алгоритм NOAA → расчёт азимута/высоты → позиционирование серво. Ночью — отключение серво. |
| `1` | **РУЧНОЙ РЕЖИМ** | Два ползунка в интерфейсе (0°–180° и 0°–90°) для прямого управления осями. |
| `2` | **КАЛИБРОВКА (90°)** | Жёсткая фиксация обоих серво на 90° для сборки и ориентации по Югу. |
| `3` | **ДЕМО** | Плавный непрерывный sweep 0°→180°→0° по горизонту + параболическая имитация дуги Солнца по вертикали. |

### Логика ночного режима (Авто)

```
sunAlt ≤ 0°  →  isNight = true  →  servoHor.detach() + servoVer.detach()
                                   (сервоприводы обесточены)

sunAlt > 0°  →  isNight = false →  smoothMove()  (плавный выход из сна)
                                →  setServos()   (обычное слежение)
```

---

## 🛠️ Установка и сборка

### Зависимости (PlatformIO)

```ini
[env:esp32doit-devkit-v1]
platform  = espressif32
board     = esp32doit-devkit-v1
framework = arduino
monitor_speed = 115200
lib_deps =
    madhephaestus/ESP32Servo @ ^1.1.2
    gyverlibs/SunPosition    @ ^1.2.0
```

### Сборка и прошивка

```bash
# 1. Клонировать репозиторий
git clone <repo-url>
cd ESP32_MG996R_Solar_Algorithm

# 2. Открыть в VS Code с расширением PlatformIO IDE

# 3. Собрать прошивку
pio run

# 4. Прошить ESP32
pio run --target upload

# 5. Открыть монитор порта (115200 бод)
pio device monitor
```

---

## 📡 Просмотр IP адреса через Serial Monitor

После включения устройства ESP32 выводит всю информацию о подключении в последовательный порт со скоростью **115 200 бод**. Это позволяет легко узнать IP адрес без дополнительной конфигурации.

### Что выводится в Serial при включении

**Рабочий режим** (устройство подключилось к вашей Wi-Fi):
```
==========================================
   ☀️  Solar Tracker OS  |  Booting...
==========================================
[WiFi] Подключение к сети: MyHomeWiFi
..........
------------------------------------------
  [РАБОЧИЙ РЕЖИМ — Подключено к WiFi]
------------------------------------------
  Сеть      : MyHomeWiFi
  IP адрес  : 192.168.1.105        ← ЭТО IP ДЛЯ БРАУЗЕРА
  GMT пояс  : +5
  Откройте браузер и введите IP адрес выше
------------------------------------------
[OK] Веб-сервер запущен на порту 80
[OK] FreeRTOS задачи запущены
==========================================
```

**Режим настройки** (устройство создало собственную точку доступа):
```
==========================================
   ☀️  Solar Tracker OS  |  Booting...
==========================================
------------------------------------------
  [РЕЖИМ НАСТРОЙКИ — Точка Доступа (AP)]
------------------------------------------
  WiFi сеть : SolarTracker
  Пароль    : 12345678
  IP адрес  : 192.168.4.1          ← ВСЕГДА ОДИН И ТОТ ЖЕ
  Откройте браузер и введите IP адрес выше
------------------------------------------
[OK] Веб-сервер запущен на порту 80
[OK] FreeRTOS задачи запущены
==========================================
```

---

### 🌐 Как использовать онлайн Serial Monitor (HHD Software)

Если у вас нет установленного PlatformIO или Arduino IDE, вы можете воспользоваться **онлайн-монитором порта** прямо в браузере.

**Ссылка:** [https://hhdsoftware.com/online-serial-port-monitor](https://hhdsoftware.com/online-serial-port-monitor)

> ⚠️ **Требование:** Браузер **Google Chrome** или **Microsoft Edge** (необходим Web Serial API). Firefox не поддерживается.

#### Пошаговая инструкция

**Шаг 1. Подключите ESP32 к компьютеру**

Подключите кабель USB (тип micro-USB или USB-C, в зависимости от платы) между ESP32 и компьютером.

**Шаг 2. Откройте онлайн монитор**

Перейдите по ссылке:
```
https://hhdsoftware.com/online-serial-port-monitor
```

**Шаг 3. Подключитесь к порту**

1. Нажмите кнопку **«Connect»** или **«Open Port»** на сайте.
2. В появившемся диалоговом окне браузера вы увидите список доступных COM-портов.
3. Выберите порт с именем типа **`USB-SERIAL CH340`**, **`Silicon Labs CP210x`** или **`USB Serial Device (COMx)`** — это и есть ваш ESP32.
4. Нажмите **«Connect»** в диалоге.

**Шаг 4. Установите скорость 115200 бод**

В настройках соединения укажите параметры:

| Параметр | Значение |
|---|---|
| **Baud Rate (скорость)** | `115200` |
| **Data Bits** | `8` |
| **Stop Bits** | `1` |
| **Parity** | `None` |

**Шаг 5. Перезапустите ESP32**

Нажмите кнопку **EN (Reset)** на плате ESP32 или отключите/подключите питание. В окне монитора появится загрузочное сообщение с IP адресом.

**Шаг 6. Скопируйте IP адрес**

Найдите в выводе строку:
```
  IP адрес  : 192.168.X.XXX
```
Скопируйте этот адрес.

**Шаг 7. Откройте веб-интерфейс**

Откройте любой браузер (на телефоне или ПК, подключённом к той же Wi-Fi сети) и введите IP адрес в адресную строку:
```
http://192.168.X.XXX
```

Откроется **дашборд Solar Tracker**.

---

## 📱 Инструкция пользователя

### Шаг 1. Первое включение (Режим настройки)

Если трекер включается впервые или не может подключиться к известной Wi-Fi сети — он автоматически создаёт **точку доступа**.

1. Включите питание трекера.
2. **Узнайте IP адрес** одним из способов:
   - Через онлайн Serial Monitor (см. раздел выше) — подключите USB к ПК
   - Или используйте стандартный IP точки доступа: **`192.168.4.1`**
3. Подключитесь к Wi-Fi сети: **`SolarTracker`**, пароль: **`12345678`**
4. Откройте браузер и введите: **`192.168.4.1`**
5. Интерфейс автоматически откроется на вкладке **«НАСТРОЙКИ»**.
6. Нажмите **«ПОИСК»**, выберите ваш Wi-Fi и введите пароль.
7. При необходимости измените GPS-координаты.
   > По умолчанию: **Астана** (Широта: `51.1333`, Долгота: `71.4333`, GMT: `+5`)
8. Нажмите **«СОХРАНИТЬ ДАННЫЕ В EEPROM»** — ESP32 перезагрузится и подключится к интернету.

### Шаг 2. Получение IP адреса в рабочем режиме

После успешного подключения к домашней Wi-Fi сети ESP32 получает **динамический IP адрес** от роутера. Чтобы его узнать:

**Способ А: Через Serial Monitor (рекомендуется)**
1. Подключите ESP32 к ПК по USB.
2. Откройте [онлайн-монитор](https://hhdsoftware.com/online-serial-port-monitor) или `pio device monitor`.
3. Перезапустите ESP32 кнопкой EN.
4. В выводе найдите строку `IP адрес  : 192.168.X.XXX`.

**Способ Б: Через роутер**
1. Откройте веб-интерфейс вашего роутера (обычно `192.168.1.1` или `192.168.0.1`).
2. Найдите раздел «Подключённые устройства» или «DHCP-клиенты».
3. Найдите устройство с именем **`espressif`** или **`ESP32`**.

**Способ В: Фиксированный IP (рекомендуется для постоянного использования)**

Назначьте ESP32 статический IP в настройках роутера по MAC-адресу устройства — тогда IP никогда не изменится.

### Шаг 3. Физическая ориентация по Югу

Для корректной работы астрономического алгоритма трекер необходимо **физически ориентировать на Юг**.

1. Войдите в режим **«КАЛИБРОВКА (90°)»** — оба сервопривода встанут по центру.
2. Возьмите телефон, откройте **приложение-компас**.
3. **Поверните весь прибор** так, чтобы солнечные панели смотрели строго на **ЮГ**.
4. Зафиксируйте прибор в этом положении.
5. Переключитесь в режим **«АВТО (СЛЕЖЕНИЕ)»**.

### Шаг 4. Управление через Дашборд

На главной странице доступны **4 кнопки управления**:

| Кнопка | Действие |
|--------|----------|
| ☀️ **АВТО (СЛЕЖЕНИЕ)** | Основной рабочий режим. Ночью кнопка становится 🌙 фиолетовой. |
| 🚀 **ДЕМО ДЛЯ ПРЕЗЕНТАЦИИ** | Трекер начинает плавно двигаться, имитируя полный световой день. |
| 🕹️ **РУЧНОЙ РЕЖИМ** | Появляются два ползунка для управления осями. |
| 📐 **КАЛИБРОВКА (90°)** | Фиксация серво по центру для сборки и ориентации. |

### Шаг 5. Вкладка «НАСТРОЙКИ»

| Параметр | Описание |
|---|---|
| **Широта (Lat)** | GPS-широта места установки (пример: `51.1333` — Астана) |
| **Долгота (Lon)** | GPS-долгота места установки (пример: `71.4333` — Астана) |
| **GMT (Пояс)** | Часовой пояс (пример: `5` для UTC+5) |
| **Мин. Наклон** | Минимальный угол вертикального серво (не менее **15°**) |
| **Макс. Наклон** | Максимальный угол вертикального серво (до **90°**) |
| **Оффсет Азимут** | Корректировка горизонтального угла в градусах |
| **Оффсет Наклон** | Корректировка вертикального угла в градусах |
| **WiFi Имя (SSID)** | Название вашей Wi-Fi сети |
| **WiFi Пароль** | Пароль от вашей Wi-Fi сети |

---

## ⚠️ Важные замечания по безопасности

> ### 🔴 Питание сервоприводов
> **Никогда** не подключайте MG996R к пинам `3.3V` или `5V` на плате ESP32 напрямую!
> Пиковый ток потребления двух MG996R может превысить **2А**, что **выведет ESP32 из строя**.
> Питание сервоприводов должно идти **напрямую от аккумулятора** (или модуля H961-U).

> ### 🟡 Ограничение углов вертикали
> Параметр **«Мин. наклон»** должен быть **не менее 15°**.
> При меньших значениях солнечные панели будут физически **задевать основание корпуса**.

---

## 📊 Технические характеристики

| Параметр | Значение |
|---|---|
| Процессор | Xtensa LX6 Dual-Core, 240 МГц |
| ОС | FreeRTOS (ESP-IDF) |
| Связь | Wi-Fi 802.11 b/g/n (2.4 ГГц) |
| Синхронизация времени | NTP (`pool.ntp.org`, `time.nist.gov`) |
| Скорость Serial Monitor | **115 200 бод** |
| Разрядность АЦП | 12 бит (0–4095) |
| Опрос АЦП | 10 усреднённых измерений |
| Шаг плавного движения | 1° / 40 мс |
| Период обновления (авто) | каждые 2000 мс |
| Период обновления (демо) | каждые 100 мс |
| Диапазон азимута | 0° – 180° |
| Диапазон высоты | 15° – 90° (настраивается) |
| Хранение настроек | EEPROM (512 байт) |
| Размер прошивки | ~21 КБ (main.cpp) |

---

## 📚 Используемые библиотеки

| Библиотека | Версия | Назначение |
|---|---|---|
| [ESP32Servo](https://github.com/madhephaestus/ESP32Servo) | `^1.1.2` | Управление сервоприводами MG996R через PWM |
| [SunPosition](https://github.com/GyverLibs/SunPosition) | `^1.2.0` | Вычисление азимута и высоты Солнца (алгоритм NOAA) |
| `WiFi.h` | built-in | Wi-Fi Station + Access Point режим |
| `WebServer.h` | built-in | HTTP сервер на порту 80 |
| `EEPROM.h` | built-in | Энергонезависимое хранение конфигурации |
| `FreeRTOS` | built-in | Многозадачность и синхронизация (Mutex) |
| `time.h` / NTP | built-in | Синхронизация времени через интернет |

---
